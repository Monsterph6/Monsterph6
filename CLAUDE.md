# CLAUDE.md

This file guides Claude Code (claude.ai/code) when working in this repository.

## Current State

This repository is **newly initialized and currently empty** — it contains only a `.gitkeep`
placeholder and a single "Initialize repository" commit. There is no source code, package
manifest, framework, or test suite to document yet.

This `CLAUDE.md` is a **placeholder**. Once real project files are added, re-run a full
repository analysis and replace the sections below with concrete, accurate details (do not
guess or invent structure that doesn't exist).

## Sections to fill in once code exists

### Overview
- What the project is and what problem it solves.

### Repository Structure
- Directory layout and the purpose of each top-level folder.
- Entry points and where core logic lives.

### Development Setup
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to build and run the project locally.

### Testing
- Test framework in use and the command to run the full suite.
- How to run a single test file/case.

### Code Conventions
- Language/style conventions, linting and formatting tools, and how to invoke them.

### Git Workflow
- Branch naming convention observed so far: `claude/<slug>` (e.g. this branch,
  `claude/claude-md-docs-rw1io9`).
- Commit message style and any PR process once established.

## Instruction for future sessions

When source files are added to this repository, analyze the codebase and rewrite this file
from scratch to reflect its actual structure, workflows, and conventions.
